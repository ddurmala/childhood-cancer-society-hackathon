{
  "name": "uniswap-swap-analysis",
  "version": "1.0.0",
  "description": "Fetches and analyzes Uniswap swap data, compares with Binance, and visualizes results.",
  "main": "server.js",
  "scripts": {
    "start": "cd server && npx sequelize db:migrate && node ../server.js",
    "dev": "nodemon server.js",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "install-all": "npm run install-client && npm run install-server"
  },
  "dependencies": {
    "@uniswap/sdk-core": "^7.5.0",
    "@uniswap/smart-order-router": "^4.17.9",
    "@uniswap/v3-sdk": "^3.24.0",
    "axios": "^1.7.9",
    "csv-parse": "^5.6.0",
    "dotenv": "^16.4.7",
    "ethers": "^5.7.2",
    "pg": "^8.13.1",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}